# Funkcje w Pythonie

Wszystko o funkcjach: def, lambda, *args/**kwargs, map, filter, reduce, any, all, zip.

## Zawartosc

| Plik | Opis |
|------|------|
| `funkcje_przewodnik.py` | Kompletny przewodnik - def, lambda, map/filter/reduce, any/all/zip |
| `co_zwraca_funkcja_metoda.py` | Sciaga: jakie typy zwracaja wbudowane funkcje i metody |
| `sum_any_all_cheatsheet.py` | Sciaga sum(), any(), all() z przykladami walidacji |

### cwiczenia/

| Plik | Opis |
|------|------|
| `cwiczenia_lambda_map_filter.py` | 10 cwiczen na lambda, map(), filter(), reduce(), any(), all(), zip() |
| `cwiczenia_funkcje_junior.py` | 10 cwiczen na poziomie junior: filtrowanie, sortowanie, comprehension |

## Kluczowe pojecia

- `def` - definiowanie funkcji
- `lambda` - funkcje anonimowe (jednolinijkowe)
- `map(func, iterable)` - zastosuj funkcje do kazdego elementu
- `filter(func, iterable)` - filtruj elementy
- `reduce(func, iterable)` - zredukuj do jednej wartosci
- `any()` / `all()` - czy cokolwiek / czy wszystko jest True
